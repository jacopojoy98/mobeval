numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
tabulate>=0.9      # optional: nicer markdown tables (plain fallback built in)
pyyaml>=6          # optional: YAML configs (JSON configs always work)
torch>=2.1          # model adapters and training (the evaluation core runs without it)
pytest>=7
# optional: h3>=4   (H3 region tokenizer for TrajGPT, or loading original TrajGPT checkpoints)
